<%= form_for(@tutorial_mode) do |f| %>
  <% if @tutorial_mode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tutorial_mode.errors.count, "error") %> prohibited this tutorial_mode from being saved:</h2>

      <ul>
      <% @tutorial_mode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
