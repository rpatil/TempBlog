<%= form_for(@tutorial_type) do |f| %>
  <% if @tutorial_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tutorial_type.errors.count, "error") %> prohibited this tutorial_type from being saved:</h2>

      <ul>
      <% @tutorial_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
